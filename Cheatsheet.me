kubectl get nodes  ## get nodes
kubectl get nodes -o wide ## gets with cluster name and ip also
kubectl create -f pod.yaml ## to create pod .
kubectl describe pod nginx  ## to troubleshoot the pod to know about pod etc
kubectl logs pod nginx ## to show the logs if any on that pod
